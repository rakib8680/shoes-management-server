export type TProduct = {
  name: string;
  price: number;
  quantity: number;
  uniqueId?: string;
  authentic?: boolean;
  brand: string;
  model: string;
  style: string;
  size: number[];
  color: string;
  photoUrl: string;
};
